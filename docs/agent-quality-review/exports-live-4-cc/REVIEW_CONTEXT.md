# Live 4 — contract-compare after numeral scorer fix

`60 days` and `sixty (60) days` are scored as the same claim. First clean live-summary read (2 scenarios × 3). Deterministic n=27 is separate.

Not an attorney close-out. n=2 does not close the workflow.
