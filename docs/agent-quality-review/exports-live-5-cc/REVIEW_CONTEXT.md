# Live 5 — expanded contract-compare summaries

16 scenarios × 3 repeats. First workflow-level live compare measurement after the n=2 scorer-bugfix check.

Config is in README (`=== Live run config ===`). Snapshot `gpt-4o-mini-2024-07-18`. Not an attorney close-out.
